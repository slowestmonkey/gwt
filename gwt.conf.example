# ─────────────────────────────────────────────────────────────────────────────
# GWT Configuration File
# ─────────────────────────────────────────────────────────────────────────────
#
# Copy this file to one of these locations (in priority order):
#   1. ./.gwt.conf           - Project-specific config
#   2. ~/.config/gwt/config  - User global config (recommended)
#   3. ~/.gwt.conf           - User global config (legacy)
#
# ─────────────────────────────────────────────────────────────────────────────

# ─────────────────────────────────────────────────────────────────────────────
# Provider Configuration
# ─────────────────────────────────────────────────────────────────────────────

# Set the default AI coding assistant
# Options: claude, opencode, aider, cursor (or any custom provider)
GWT_PROVIDER=claude

# ─────────────────────────────────────────────────────────────────────────────
# Debug Mode
# ─────────────────────────────────────────────────────────────────────────────

# Enable debug output (shows internal operations)
# Set to 1 to enable, 0 to disable
# GWT_DEBUG=0

# ─────────────────────────────────────────────────────────────────────────────
# Custom Providers
# ─────────────────────────────────────────────────────────────────────────────
#
# You can define custom providers here or in ~/.config/gwt/providers.d/*.zsh
#
# Format: GWT_PROVIDERS[name]="command|safe_flags|dangerous_flags|Display Name"
#
# Fields:
#   - command: The CLI command to run
#   - safe_flags: Flags for restricted/safe mode (optional)
#   - dangerous_flags: Flags to skip all prompts (optional)
#   - Display Name: Human-readable name
#
# Examples:
#
# # Provider with no special flags
# GWT_PROVIDERS[myai]="myai-cli|||My AI Tool"
#
# # Provider with safe mode only
# GWT_PROVIDERS[safeai]="safeai|--restricted||Safe AI"
#
# # Provider with both modes
# GWT_PROVIDERS[fullai]="fullai|--safe|--yolo|Full AI"
#
# ─────────────────────────────────────────────────────────────────────────────

# ─────────────────────────────────────────────────────────────────────────────
# Claude Code Safe Mode Tools
# ─────────────────────────────────────────────────────────────────────────────
#
# When using Claude Code with -s/--safe flag, these tools are allowed:
#
# GWT_ALLOWED_TOOLS=(
#   "Read" "Edit" "Write" "Grep" "Glob"
#   "Bash(git:*)" "Bash(npm:*)" "Bash(ls:*)" "Bash(cat:*)"
# )
#
# Uncomment and modify to customize allowed tools in safe mode.
# ─────────────────────────────────────────────────────────────────────────────
